import { describe, expect, it } from "vitest"

// TODO: Add tests for the subtitle burner app at /ass
describe("/+page.svelte", () => {
    it("placeholder", async () => {
        expect(true).toBe(true)
    })
})
